import React from 'react';

import './Header.style.scss';

const Header = () => {
  return (
    <header>
      <span className="title">Test User React</span>
    </header>
  );
};

export default Header;
